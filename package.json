{
  "name": "first_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --browser chrome --reporter mocha-junit-reporter --reporter-options mochaFile=cypress/Test-results.xml",
    "test:chrome": "cypress run --browser chrome "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.13.0",
    "cypress-junit-reporter": "^1.3.1",
    "mochawesome": "7.0.0",
    "mochawesome-merge": "4.1.0",
    "mochawesome-report-generator": "6.0.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.4.0",
    "mocha-junit-reporter": "^2.2.0"
  }
}
